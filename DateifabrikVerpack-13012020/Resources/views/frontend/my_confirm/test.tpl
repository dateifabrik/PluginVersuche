{* http://192.168.33.89/shop/MyConfirm/test wäre eine eigene Route, sonst an die Vorgaben im Theme-Ordner halten (ruft die testAction und somit die test.tpl auf) *}

{* für den Ordner-Namen wird aus CamelCase-Schriftweise die snake_case-Schriftweise (aus Event-Name "MyConfirm" wird "my_confirm) *}
{* index-Datei für das Event MyConfirm *}
{* Hier könn(t)en die Original-Blöcke überschrieben werden *}

<h1>Hallo Welt</h1>
<p>Es können hier (bisher) keine Smarty-Variablen aus Shopware oder dem Plugin angezeigt werden.</p>

<br />
<p>Hier können meiner Meinung nach nur Funktionalitäten hinterlegt werden, die dann wiederum den Views zugeordnet werden könnten.</p>

